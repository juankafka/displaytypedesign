doctype
html
    | {% include head.html %}
    body.ajax-loading
      | {% include header.html %}
      .page
        .page__content
          section.intro
            .wrap
              | {% if page.teaser  %}
              img(src="{{ site.baseurl }}{{ page.teaser }}")
              | {% endif %}
              p
              | {{ content | markdownify }}
          | {% assign projects = site.projects %}
          | {% include project_grid.html %}

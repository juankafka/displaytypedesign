doctype
html
    | {% include head.html %}
    body.ajax-loading
      | {% include header.html %}
      .page
        .page__content
          | {% if page.class %}
          | {% assign projects = site.projects | where:'class', page.class %}
          | {% else %}
          | {% assign projects = site.projects %}
          | {% endif %}

          | {% include project_grid.html %}